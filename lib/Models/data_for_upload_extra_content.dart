class DataForUploadExtraContent {
  const DataForUploadExtraContent(this.participatingStartDate, this.participatingEndDate, this.participatingQuestionTitle);

  final DateTime participatingStartDate;
  final DateTime participatingEndDate;
  final String participatingQuestionTitle;
}